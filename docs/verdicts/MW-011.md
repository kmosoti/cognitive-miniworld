# MW-011 — Forward model

**Verdict:** accepted on 2026-08-28 in package 0.9.0 under ADR-022’s frozen gate.

**Proper score.** The categorical Brier rule strictly preferred the truthful binary forecast in its expected-loss fixture. Across seeds 1000–1099, learned pre-shift loss was `0.0666666030883789` versus identity loss `2.0`, an improvement of `1.933333396911621` over the `0.25` threshold.

**Regime shift.** After the action transition changed, learned loss was `0.13320315678902273` versus frozen-model loss `2.0`, an improvement of `1.8667968432109774`; every active row recovered in two ticks, inside the four-tick limit.

**Delayed decision.** Models trained from hashed public consume-once and wait episodes selected by predicted five-tick safety on canonical `delayed_poison`. Viability AUC rose from `0.1273170731707317` to `0.14878048780487807`, a strict `0.02146341463414636` gain for every paired seed.

**Verification.** Ruff, ty, source/wheel build, all 409 tests in the `0.9.0` issue slice, lock validation, and the 226-node graph passed. Canonical 100-seed evidence was 95,416 bytes with SHA-256 `dc56c0cc15498256d8b18a7245c9ebdbf00acc623edb5b47fa29cb66859f9a13` and 100 seed-bound transition traces.

**Deviation.** None for issue acceptance; the delayed selector remains evaluator-only, so reusable multi-objective arbitration is still reserved for MW-014.
