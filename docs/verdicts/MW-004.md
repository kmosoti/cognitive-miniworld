# MW-004 — World kernel verdict

**Verdict:** acceptance criteria satisfied on 2026-08-27.

- Added an immutable, evaluator-only `WorldState` over a configurable 2D grid.
- `transition(state, action, rng)` is referentially deterministic and advances an immutable RNG continuation without mutating any input.
- Energy and integrity obey ADR-011 bounds, soft-band margin, and irreversible hard terminal floors.
- Authoritative action rules cover all seven EPIC actions, duration, viability cost, hazards, slips, retreat, and hidden precondition failure.
- Consumable units are conserved exactly; delayed hidden resource effects remain deterministic.
- Four public `ObservationEnvelope` channels expose noisy local evidence, interoception, time, and efference copy without resource quality or hidden state.
- Property tests cover arbitrary action sequences, conservation, resource bounds, viability bounds, and grid closure.
- The architecture gate rejects any `agents/` or `primitives/` import, annotation, or alias that reaches `cmw.kernel` or `WorldState`.
- Locked Ruff, ty, pytest, package build, replay smoke, and evidence-graph validation pass.
- Deviations: none.
